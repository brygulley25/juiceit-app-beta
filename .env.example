# JuiceIT Environment Variables
# Copy this file to .env and fill in your actual values

# App Configuration
EXPO_PUBLIC_APP_SCHEME=myapp

# Supabase Configuration (Required)
# Get these from your Supabase project dashboard
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Analytics (Supabase)
# Analytics events are stored in the analytics_events table
# No additional configuration needed - uses same Supabase connection

# Stripe Configuration (Required for payments)
# Create these in your Stripe dashboard
EXPO_PUBLIC_STRIPE_PAYMENT_LINK=https://buy.stripe.com/your-payment-link
EXPO_PUBLIC_STRIPE_CUSTOMER_PORTAL=https://billing.stripe.com/p/login/your-portal-link

# Server-side Secrets (Edge Functions Only - DO NOT expose to client)
# These are automatically available in Supabase Edge Functions
# OPENAI_API_KEY=sk-your-openai-key (set in Supabase dashboard)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key (auto-provided)
# SUPABASE_URL=your-supabase-url (auto-provided)

# Development Notes:
# - All EXPO_PUBLIC_* variables are included in the client bundle
# - Never put private keys in EXPO_PUBLIC_* variables
# - Server-side secrets are managed in Supabase Edge Function environment